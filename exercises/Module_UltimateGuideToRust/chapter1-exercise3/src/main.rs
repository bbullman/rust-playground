/*
 * Write a program that prints the result of doing 5 + 3, then 6 - 2, then multiplying the two results together.

    Do it once using multiple variables.

    Do it once using a single variable.

    Do it once using no variables at all.
*/

fn main(){
    println!("{}", (5 + 3) * (6 - 2));
}
